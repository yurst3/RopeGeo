/** Explore trail line colors — not tied to map light preset. */
export const EXPLORE_TRAIL_LINE_COLORS = {
  unfocused: "#6b7280",
  focused: "#000000",
} as const;

/** Prevent Mapbox Standard light presets from dimming trail paint. */
export const EXPLORE_TRAIL_LINE_EMISSIVE_STRENGTH = 1;
